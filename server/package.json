{
  "name": "pahal-ngo-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js" // Requires nodemon: npm i -D nodemon
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3", // Or later version
    "express": "^4.18.2",
    "mongoose": "^7.0.3", // Or later version
    "nodemailer": "^6.9.1" // Or later version
  },
  "devDependencies": {
    "nodemon": "^2.0.22" // Or later version
  }
}
